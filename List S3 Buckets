import boto3
s3 = boto3.client('s3')
def lambda_handler(event, context):
    response = s3.list_buckets()
    print(f"s3 buckets: {response}")
    return{
        'statuscode': 200,
        'body': "S3 buckets are listed: {response}"
    }
